<%= provide(:title, "Student Index") %>
<%= provide(:intro, "Teacher can choose to create a new student, update an existing one or delete a student from the list.") %>
<ul>
  <li>Student</li>
    <ol>
    <% @students.each do |student|  %>
    <li> <%= student.name  %></li>
     <%= link_to 'Edit', edit_student_path(student)  %>
     <%= link_to 'Delete', student, method: :delete, data: {confirm: 'Are you sure?'}  %>
     <%= link_to 'Courses', course_rosters_path %>
    <% end  %>
  </ol>
</ul>
<br>
<h2><p>Want to create a new student? Click here: <%= link_to 'New', new_student_path  %></h2>
