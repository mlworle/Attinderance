<% provide(:title, "Edit Course") %>
<% provide(:intro, "This page will allow the user/course to edit or update the data for their own profile. Right now that is only one string field for name.") %>

<p><%= render 'form' %>
   <%= link_to 'Back', courses_path %>
</p>
